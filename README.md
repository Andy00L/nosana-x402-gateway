# nosana-x402-gateway

x402 payment gateway for Nosana GPU compute. An AI agent calls `POST /rent`,
receives `402 Payment Required`, pays USDC on Solana, and gets back a running
GPU deployment. Status: planning done, Phase 1 build starting. See
`docs/x402-execution-plan.md`.
